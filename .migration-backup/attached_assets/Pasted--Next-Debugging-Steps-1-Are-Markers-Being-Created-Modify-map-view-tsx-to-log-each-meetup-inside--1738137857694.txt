🔍 Next Debugging Steps
1. Are Markers Being Created?
Modify map-view.tsx to log each meetup inside the .map() function:

tsx
Copy
Edit
{meetups?.map((meetup) => {
  console.log("Creating marker:", meetup.title, meetup.latitude, meetup.longitude);
  return (
    <Marker
      key={meetup.id}
      position={{
        lat: Number(meetup.latitude),
        lng: Number(meetup.longitude)
      }}
      onClick={() => console.log("Marker clicked:", meetup)}
    />
  );
})}
✅ If this does NOT log anything: meetups is likely empty when map-view.tsx renders.

✅ If it logs correct lat/lng values but markers don’t appear: Go to step 2.

2. Check If meetups Is Empty When MapView Renders
Modify map-view.tsx to log meetups at the start of rendering:

tsx
Copy
Edit
console.log("MapView rendering - meetups:", meetups);
✅ If meetups is [] (empty) at first render, but later logs the correct data, the issue is likely that markers are being rendered before meetups is available.

✅ Solution: Add a condition to only render markers when meetups exist:

tsx
Copy
Edit
{meetups.length > 0 &&
  meetups.map((meetup) => (
    <Marker key={meetup.id} position={{ lat: meetup.latitude, lng: meetup.longitude }} />
  ))}
3. Verify Google Maps API Is Fully Loaded Before Rendering
Modify map-view.tsx:

tsx
Copy
Edit
<LoadScript googleMapsApiKey={import.meta.env.VITE_GOOGLE_MAPS_API_KEY} onLoad={() => console.log("Google Maps API Loaded")}>
✅ If "Google Maps API Loaded" never logs, the API isn’t loading correctly.

4. Confirm Latitude & Longitude Are Numbers
Modify map-view.tsx:

tsx
Copy
Edit
console.log("Meetup Coordinates:", meetup.latitude, meetup.longitude, typeof meetup.latitude, typeof meetup.longitude);
✅ If it logs "string string", fix it:

tsx
Copy
Edit
lat: Number(meetup.latitude),
lng: Number(meetup.longitude),
5. Center Map on First Meetup
If markers exist but are off-screen, modify defaultCenter:

tsx
Copy
Edit
const defaultCenter = meetups.length > 0 
  ? { lat: Number(meetups[0].latitude), lng: Number(meetups[0].longitude) }
  : { lat: 37.7749, lng: -122.4194 };  // Default to SF
✅ This ensures the map starts centered on a valid marker.

6. Hard Refresh & Restart Vite
bash
Copy
Edit
npm run dev
Then force refresh the browser (Ctrl + Shift + R or Cmd + Shift + R on Mac).